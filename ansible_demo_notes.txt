Run install.sh
Show Host and env in browser
create ansible-playbook alias
Run RemoveLinux playbook
Show multipath -ll
Create some files in /mnt/demo-test
Run CreateActiveCluster playbook
Show that there are now 4 paths to linux host (fa1 and fa2)
Run The CreateDev playbook
Show that /mnt/demo-test and /mnt/ansible-test are the same.


